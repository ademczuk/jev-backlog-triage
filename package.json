{
  "name": "jev-backlog-triage",
  "private": true,
  "type": "module",
  "scripts": {
    "triage": "node --env-file-if-exists=.env.local --experimental-strip-types src/triage.ts"
  },
  "dependencies": {
    "ai": "^7.0.0"
  },
  "engines": {
    "node": "22.x"
  }
}
